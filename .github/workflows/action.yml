name: Index workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/ci-workflow.yml
    secrets: inherit
  deploy:
    needs: build
    uses: ./.github/workflows/cd-workflow.yml
    secrets: inherit